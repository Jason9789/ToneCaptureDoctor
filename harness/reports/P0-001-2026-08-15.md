# P0-001 bootstrap verification — 2026-08-15

## 작업

- Phase: P0
- Issue: P0-001 — npm workspace, React/Vite shell, CI
- 브랜치: `feat/p0-001-bootstrap`
- 런타임: bundled Node.js `v24.19.0`, npm `11.12.1`
- 실제 오디오 사용: 없음
- 오디오 서버 업로드: 없음

## 구현 범위

- npm workspace와 `apps/web` React/Vite placeholder
- Node 24 `.nvmrc`, npm 11 engine 범위, lockfile
- strict TypeScript, ESLint, Prettier, Vitest, Playwright
- Signal Health placeholder dashboard
- GitHub Actions CI
- CONTRIBUTING와 자동 검증 안내

오디오 권한, 장치 선택, DSP, 서버·로그인·telemetry, Tauri, Extension은 구현하지 않았다.

## 자동 검증

- `npm install`: PASS — 초기 lockfile 생성, audit 취약점 0개
- `npm run format:check`: PASS
- `npm run lint`: PASS
- `npm run typecheck`: PASS
- `npm test`: PASS — 1 test
- `npm run test:e2e`: PASS — Chromium smoke test 1 test
- `npm run build`: PASS — Vite production build
- `npm ci`: PASS — lockfile 기반 clean install, audit 취약점 0개
- clean install 후 동일 format/lint/typecheck/unit/e2e/build: PASS
- `git diff --check`: PASS — staged diff 기준

## 독립 검토

- 읽기 전용 서브에이전트 `Heisenberg` (`01a00554-6c04-7513-8a9d-3d524a99563b`): BLOCKED —
  제한 시간 내 결과를 반환하지 않아 파일 수정 없이 종료함
- 서브에이전트 검토 결과를 자동 PASS로 대체하지 않음

## 수동·사람 Gate

- 실제 macOS 새 clone `npm ci`와 dev/preview 확인: PENDING
- Windows 새 clone 설치와 동일 UI 확인: PENDING
- GitHub Actions green 3회: PENDING
- 실제 인터페이스·권한·오디오 장비 검증: 이 이슈 범위 밖이며 이후 Phase에서 수행

자동 검증은 통과했지만 사람 Gate가 끝나지 않았으므로 `harness/state.json`의
`lastCompletedGate`는 `null`로 유지한다.
